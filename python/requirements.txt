# ====================================
# QUICORD CORE DEPENDENCIES (Required)
# ====================================
docling>=1.0.0
docling-ibm-models[opencv-python]>=3.10.1
easyocr>=1.7.0
opencv-python>=4.8.0
python-docx>=1.2.0
reportlab>=4.0.0
pypdf>=6.0.0
ebooklib>=0.20
openpyxl>=3.1.0
Pillow>=11.0.0

# ====================================
# OPTIONAL: Local LLM for AI text restructuring
# ====================================
# Install these ONLY if you want AI-powered paragraph reordering.
# This is a SECONDARY feature - Quicord works fine without it!
#
# Total download: ~3-8GB (model + dependencies)
# RAM needed: 4-16GB (depending on model choice)
#
# To install:
#   pip install transformers torch accelerate psutil bitsandbytes
#   python python/install_llm_optional.py
#
# Already installed:
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.20.0
psutil>=5.9.0
bitsandbytes>=0.41.0
