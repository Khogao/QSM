import { useState } from 'react';
import { DocumentManagement } from './components/DocumentManagement';

function App() {
  const [stats, setStats] = useState({ documents: 0, size: '0 MB' });

  return (
    <div className="min-h-screen flex flex-col bg-gray-50">
      {/* Header */}
      <header className="bg-white shadow-sm p-4 border-b">
        <div className="flex items-center justify-between">
          <div>
            <h1 className="text-2xl font-bold text-gray-900">QueryMaster Free</h1>
            <p className="text-sm text-gray-500">Local Document Management & RAG</p>
          </div>
          <div className="flex gap-3">
            <div className="px-3 py-1 bg-blue-50 rounded-md border border-blue-200">
              <span className="text-xs text-gray-600">Docs:</span>
              <span className="ml-1 text-sm font-semibold text-blue-600">{stats.documents}</span>
            </div>
            <div className="px-3 py-1 bg-purple-50 rounded-md border border-purple-200">
              <span className="text-xs text-gray-600">Size:</span>
              <span className="ml-1 text-sm font-semibold text-purple-600">{stats.size}</span>
            </div>
          </div>
        </div>
      </header>

      {/* Main Layout with Sidebar */}
      <div className="flex-1 flex overflow-hidden">
        {/* Sidebar */}
        <aside className="w-64 bg-white border-r border-gray-200 flex flex-col">
          <div className="p-4 border-b">
            <h2 className="text-sm font-semibold text-gray-700 mb-3">📁 Folders</h2>
            <div className="space-y-1">
              <div className="px-3 py-2 rounded-md bg-blue-50 border border-blue-200 text-sm font-medium text-blue-900 cursor-pointer">
                📂 All Documents
              </div>
              <div className="px-3 py-2 rounded-md hover:bg-gray-100 text-sm text-gray-700 cursor-pointer">
                Recent
              </div>
            </div>
          </div>

          <div className="p-4 border-b">
            <h2 className="text-sm font-semibold text-gray-700 mb-3">⚙️ Settings</h2>
            <div className="space-y-2 text-xs text-gray-600">
              <div className="flex items-center justify-between">
                <span>Storage</span>
                <span className="text-green-600 font-medium">Local</span>
              </div>
              <div className="flex items-center justify-between">
                <span>RAG Engine</span>
                <span className="text-gray-400">Disabled</span>
              </div>
            </div>
          </div>

          {/* Pro Upgrade Banner */}
          <div className="mt-auto p-4 bg-gradient-to-br from-blue-500 to-purple-600 text-white">
            <h3 className="text-sm font-bold mb-2">🚀 Upgrade to Pro</h3>
            <p className="text-xs text-blue-100 mb-3">
              Unlock network drives, RAG queries, and more!
            </p>
            <button className="w-full px-3 py-2 bg-white text-blue-600 rounded-md text-xs font-semibold hover:bg-blue-50 transition-colors">
              Learn More
            </button>
          </div>
        </aside>

        {/* Main Content Area */}
        <main className="flex-1 overflow-auto p-6">
          <div className="max-w-5xl mx-auto space-y-6">
            <DocumentManagement onStatsUpdate={setStats} />
          </div>
        </main>
      </div>
    </div>
  );
}

export default App;
